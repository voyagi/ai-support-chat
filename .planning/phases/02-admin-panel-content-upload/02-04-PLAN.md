---
phase: 02-admin-panel-content-upload
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Login page looks polished with gradient, centered card, branding"
    - "Wrong password shows shake animation and error message"
    - "Upload zone accepts drag-and-drop and shows status badges"
    - "Document table is sortable and rows expand to show chunks"
    - "Delete confirmation dialog works and removes document"
    - "Seed script loads all 18 FlowBoard docs into database"
    - "Admin dashboard shows seeded documents with correct chunk counts"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete admin panel flow end-to-end: login, upload, view, expand, delete, and seeded content.

Purpose: Phase 2 has visual and interactive elements that require human verification. This plan seeds the database with FlowBoard content and confirms the full admin experience works as expected.
Output: Verified, working admin panel with populated knowledge base.
</objective>

<execution_context>
@C:\Users\Eagi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Eagi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-admin-panel-content-upload/02-CONTEXT.md
@.planning/phases/02-admin-panel-content-upload/02-01-SUMMARY.md
@.planning/phases/02-admin-panel-content-upload/02-02-SUMMARY.md
@.planning/phases/02-admin-panel-content-upload/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed database with all FlowBoard demo documents</name>
  <files></files>
  <action>
    Run the seed script to load all 18 FlowBoard documents into Supabase:

    1. First verify env vars are set: check .env.local has OPENAI_API_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY, ADMIN_PASSWORD, SESSION_SECRET

    2. Run dry-run first to verify all docs process correctly:
       `npm run seed -- --dry-run`
       Expected: 18 documents processed, ~200+ total chunks

    3. Run live seed:
       `npm run seed`
       Expected: 18 documents seeded successfully, no errors

    4. Verify by starting dev server and checking admin panel shows documents:
       `npm run dev`
       Navigate to /admin/login, enter password, verify document table shows 18 documents with chunk counts

    NOTE: If ADMIN_PASSWORD or SESSION_SECRET are not set in .env.local, add them before proceeding. Generate SESSION_SECRET with `openssl rand -hex 32` or similar.
  </action>
  <verify>
    - Seed script reports 18 documents processed with 0 failures
    - Admin dashboard shows 18 documents in the table
    - Each document has non-zero chunk count
  </verify>
  <done>
    Database is populated with 18 FlowBoard demo documents. All documents visible in admin dashboard with correct chunk counts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete admin panel: password-gated login, document upload with drag-and-drop, sortable document table with expandable chunk preview, document deletion, and 18 pre-loaded FlowBoard demo documents.
  </what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and test these flows:

    **1. Login Flow (http://localhost:3000/admin/login)**
    - [ ] Page shows centered card with gradient background and branding
    - [ ] Enter wrong password: see shake animation and "Incorrect password" message
    - [ ] Enter correct password: redirects to /admin dashboard

    **2. Dashboard View (http://localhost:3000/admin)**
    - [ ] Page shows "Knowledge Base" heading with document count badge
    - [ ] Document table lists 18 FlowBoard documents
    - [ ] Table columns: title, upload date, chunk count
    - [ ] Click column headers: table re-sorts correctly

    **3. Chunk Expansion**
    - [ ] Click a document row: chunks appear below the row
    - [ ] Chunks show section heading and content preview
    - [ ] Click again: chunks collapse

    **4. Document Upload**
    - [ ] Drag a .txt or .md file into the upload zone
    - [ ] See "Processing..." status badge while uploading
    - [ ] Badge changes to "Ready" on success
    - [ ] New document appears in the table with correct chunk count

    **5. Document Delete**
    - [ ] Click delete button on a document
    - [ ] Confirmation dialog appears with document name
    - [ ] Confirm: document disappears from table
    - [ ] Reject: document remains

    **6. Session Persistence**
    - [ ] Refresh the page: still logged in (no re-login required)
    - [ ] Click logout: redirects to login page
    - [ ] Visit /admin directly: redirects to login

    **7. Protection**
    - [ ] Open incognito window, visit /admin: redirects to login
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Seed script ran successfully with 18 documents
2. All 7 verification categories above confirmed by human
3. No console errors during testing
4. Build still passes: `npm run build`
</verification>

<success_criteria>
- Database has 18 FlowBoard documents with embeddings
- Admin login flow works with correct and incorrect password
- Document upload flow works end-to-end (file to embedded chunks)
- Document table is sortable with expandable chunk previews
- Document deletion works with confirmation
- Session persists across page refreshes
- Route protection redirects unauthenticated users
- Human has approved the visual and interactive quality
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-panel-content-upload/02-04-SUMMARY.md`
</output>
