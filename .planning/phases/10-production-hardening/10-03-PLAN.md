---
phase: 10-production-hardening
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/opengraph-image.tsx
  - src/app/icon.png
  - src/app/apple-icon.png
  - .env.example
autonomous: false

must_haves:
  truths:
    - "Site has FlowBoard favicon visible in browser tab"
    - "Sharing the URL on social media shows branded OG card with FlowBoard name and description"
    - "Landing page footer has builder credit with link to Upwork profile"
    - "App is deployed and accessible at a public Vercel URL"
    - "All environment variables are configured in Vercel dashboard"
    - "Rate limiting works in production (returns 429 after 20 requests/hour)"
    - "Sandbox document uploads do not count toward the chat rate limit counter"
  artifacts:
    - path: "src/app/opengraph-image.tsx"
      provides: "Dynamic OG image with FlowBoard branding"
    - path: "src/app/icon.png"
      provides: "32x32 favicon"
    - path: "src/app/layout.tsx"
      provides: "Updated metadata with OG and Twitter card config"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/opengraph-image.tsx"
      via: "Next.js auto-discovery of opengraph-image file convention"
      pattern: "opengraph-image"
    - from: "src/app/page.tsx"
      to: "Upwork profile"
      via: "Footer link"
      pattern: "upwork\\.com|Built by"
---

<objective>
Rebrand the app as FlowBoard with proper OG metadata, favicon, builder credit, and deploy to Vercel with all environment variables configured.

Purpose: Makes the demo look like a real product (not a student project), enables social sharing with branded preview cards, and puts the app live at a public URL for Upwork prospects.

Output: FlowBoard branding (OG image, favicon, metadata), builder credit in footer, deployed app on Vercel.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-hardening/10-RESEARCH.md
@src/app/layout.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FlowBoard branding, OG image, favicon, and builder credit</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/opengraph-image.tsx
    src/app/icon.png
    src/app/apple-icon.png
    .env.example
  </files>
  <action>
1. Create `src/app/opengraph-image.tsx`:
   - Use `next/og` `ImageResponse` API
   - Set `runtime = "edge"`
   - Set `alt = "FlowBoard - AI Customer Support"`
   - Set `size = { width: 1200, height: 630 }`
   - Set `contentType = "image/png"`
   - Design: gradient background (blue-purple, e.g. #667eea to #764ba2), centered layout with:
     - "FlowBoard" text at 72px bold white
     - "AI Customer Support" at 32px white (90% opacity)
     - "Try the demo - Instant answers from your knowledge base" at 24px white (70% opacity)
   - Use inline styles (JSX style objects), no external CSS

2. Create `src/app/icon.png`:
   - Generate a simple 32x32 favicon programmatically using a script:
     - Write a Node.js script to temp that creates a simple blue square with "F" letter as a PNG
     - Alternatively, use the Next.js icon.tsx file convention with ImageResponse for a dynamic favicon:
       ```
       // src/app/icon.tsx
       import { ImageResponse } from 'next/og'
       export const runtime = 'edge'
       export const size = { width: 32, height: 32 }
       export const contentType = 'image/png'
       export default function Icon() {
         return new ImageResponse(
           <div style={{ background: '#2563eb', width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', borderRadius: '6px' }}>
             <span style={{ color: 'white', fontSize: '20px', fontWeight: 'bold' }}>F</span>
           </div>,
           { ...size }
         )
       }
       ```
     - Use icon.tsx instead of icon.png (same for apple-icon.tsx at 180x180)
   - This approach avoids needing external image generation tools

3. Update `src/app/layout.tsx`:
   - Change title from "AI Support Chat" to "FlowBoard - AI Customer Support"
   - Update description to "Try the demo - Get instant answers from your knowledge base with AI-powered chat"
   - Add comprehensive metadata export:
     ```
     metadata: {
       title: { default: "FlowBoard - AI Customer Support", template: "%s | FlowBoard" },
       description: "...",
       metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000'),
       openGraph: {
         title: "FlowBoard - AI Customer Support",
         description: "...",
         siteName: "FlowBoard",
         locale: "en_US",
         type: "website",
       },
       twitter: {
         card: "summary_large_image",
         title: "FlowBoard - AI Customer Support",
         description: "...",
       },
     }
     ```
   - The opengraph-image.tsx file is auto-discovered by Next.js, no explicit reference needed

4. Update `src/app/page.tsx`:
   - Change "AI Support Chat" heading to "FlowBoard"
   - Update subtitle to focus on customer support value prop
   - Update footer section:
     - Replace "Built as a portfolio demo" with: "Built by [Your Name]" with a link to Upwork profile (use an environment variable NEXT_PUBLIC_UPWORK_URL or hardcode the profile link for now)
     - Add subtle text: "AI-powered customer support for any business"
     - Keep Admin Panel link but make it less prominent
   - Update page metadata export title to "FlowBoard - AI Customer Support Demo"

5. Update `.env.example`:
   - Add new env vars with comments:
     ```
     # Upstash Redis (rate limiting)
     UPSTASH_REDIS_REST_URL=
     UPSTASH_REDIS_REST_TOKEN=

     # Sandbox mode
     NEXT_PUBLIC_SANDBOX_ENABLED=false

     # Vercel Cron secret
     CRON_SECRET=

     # App URL (for OG metadata)
     NEXT_PUBLIC_APP_URL=http://localhost:3000
     ```
  </action>
  <verify>
    - `npm run check` passes
    - `npm run build` compiles without errors
    - opengraph-image.tsx exports a valid default function
    - icon.tsx exports a valid default function with correct size
    - layout.tsx has openGraph and twitter metadata
    - page.tsx has "FlowBoard" branding and builder credit with link
    - .env.example includes all new environment variables
  </verify>
  <done>
    - OG image generates branded FlowBoard preview card
    - Favicon shows blue "F" in browser tab
    - Layout metadata includes openGraph and twitter card config
    - Landing page shows FlowBoard branding with builder credit link
    - .env.example documents all new environment variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel via CLI</name>
  <files>
    (no files - CLI deployment)
  </files>
  <action>
1. Verify build passes locally:
   ```
   npm run build
   ```
   If build fails, fix errors before deploying.

2. Deploy to Vercel:
   ```
   npx vercel --yes
   ```
   This creates a preview deployment. Capture the deployment URL from the output.

3. If the project is not yet linked to Vercel:
   - The CLI will prompt for project setup
   - Select the voyagi account/team
   - Project name: upwork-ai-chatbot
   - Framework: Next.js (auto-detected)
   - Root directory: ./

4. After initial deploy, promote to production:
   ```
   npx vercel --prod --yes
   ```

5. Note: Environment variables must be configured in Vercel dashboard BEFORE the app will work fully. The human verification step below will verify this.

6. Print the deployment URL for the user.
  </action>
  <verify>
    - `npx vercel --prod --yes` exits 0
    - Deployment URL is accessible via browser
    - No build errors in Vercel deployment logs
  </verify>
  <done>
    - App is deployed to a public Vercel URL (*.vercel.app)
    - Build completed successfully on Vercel
    - URL is printed for user to configure env vars and verify
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify production deployment end-to-end</name>
  <files>(human verification - no files modified)</files>
  <action>
Human verification of complete production deployment. Configure env vars in Vercel dashboard, run Supabase migration, then verify all features work in production.

**Pre-verification setup (Vercel Dashboard):**
1. Go to Vercel project settings -> Environment Variables
2. Add ALL required env vars for Production:
   - OPENAI_API_KEY (from OpenAI dashboard)
   - NEXT_PUBLIC_SUPABASE_URL (from Supabase)
   - NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY (from Supabase)
   - SUPABASE_SECRET_KEY (from Supabase)
   - UPSTASH_REDIS_REST_URL (from Upstash Console)
   - UPSTASH_REDIS_REST_TOKEN (from Upstash Console)
   - CRON_SECRET (generate with: openssl rand -hex 32)
   - NEXT_PUBLIC_SANDBOX_ENABLED=true (to enable sandbox)
   - NEXT_PUBLIC_APP_URL=(your Vercel deployment URL)
   - ADMIN_PASSWORD (from .env.local)
3. Redeploy after adding env vars: `npx vercel --prod --yes`

**Supabase migration:**
4. Run migration 010_sandbox_tenant_isolation.sql in Supabase SQL editor

**Verification steps:**
5. Visit the Vercel URL - verify FlowBoard branding loads
6. Check browser tab - verify favicon shows blue "F"
7. Check landing page footer - verify builder credit with link
8. Go to /chat - send a few messages about FlowBoard
9. Check response headers (DevTools -> Network) for X-RateLimit-Remaining
10. If sandbox enabled: try uploading a document via the "Try with your own docs" panel
11. Ask a question about your uploaded document
12. Share the URL on a platform preview tool (e.g. https://www.opengraph.xyz/) to verify OG card
13. Log into admin panel and check for cost alert banner (if any usage)
14. Sandbox + rate limit interaction test: Upload a document in sandbox mode, then verify that the upload itself does NOT count toward the chat rate limit counter (upload goes to /api/sandbox/upload, not /api/chat). Send a chat message after uploading and confirm X-RateLimit-Remaining reflects only chat messages, not uploads.
  </action>
  <verify>User confirms all 14 verification steps pass</verify>
  <done>
    - App is live at public Vercel URL with all features functional
    - FlowBoard branding, OG image, favicon all render correctly
    - Rate limiting active in production
    - Sandbox upload works for prospects
    - Builder credit visible in footer
  </done>
</task>

</tasks>

<verification>
1. `npm run check` passes (Biome lint + format)
2. `npm run build` compiles locally without errors
3. Vercel deployment is live and accessible
4. OG image renders correctly (check via sharing tools)
5. Favicon visible in browser tab
6. Rate limiting returns proper headers on chat API
7. FlowBoard branding visible throughout the app
8. Builder credit in footer links to Upwork profile
</verification>

<success_criteria>
- App is live at a public Vercel URL with FlowBoard branding
- Social sharing produces a branded OG card
- All environment variables are configured in production
- Rate limiting and cost controls are active in production
- Sandbox mode allows prospect uploads when enabled
- The deployed app represents a professional portfolio piece
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-03-SUMMARY.md`
</output>
