---
phase: 09-out-of-kb-handling
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/chat/ContactForm.tsx
  - src/components/chat/MessageBubble.tsx
  - src/components/chat/ChatWindow.tsx
  - src/app/admin/contacts/page.tsx
  - src/app/admin/contacts/ContactsTable.tsx
  - src/app/admin/layout.tsx
autonomous: false

must_haves:
  truths:
    - "When the bot cannot answer from KB, an inline contact form appears below the 'I don't have information' message"
    - "Contact form collects name and email, auto-includes the original question, and submits to /api/contact"
    - "After submission, form shows success confirmation and cannot be resubmitted"
    - "Admin can view all contact submissions with status, name, email, question, and timestamp in /admin/contacts"
    - "Admin nav includes a 'Contacts' link to the submissions page"
  artifacts:
    - path: "src/components/chat/ContactForm.tsx"
      provides: "Inline contact form component with name/email fields, submission state, success/error handling"
      min_lines: 50
    - path: "src/components/chat/MessageBubble.tsx"
      provides: "Renders ContactForm when message contains contact-form data part"
      contains: "ContactForm"
    - path: "src/components/chat/ChatWindow.tsx"
      provides: "Passes message parts (including data parts) to MessageBubble"
      contains: "parts"
    - path: "src/app/admin/contacts/page.tsx"
      provides: "Server Component listing contact submissions from database"
      contains: "contact_submissions"
    - path: "src/app/admin/contacts/ContactsTable.tsx"
      provides: "Client component rendering contact submissions table with status badges"
      min_lines: 40
    - path: "src/app/admin/layout.tsx"
      provides: "Admin nav with Contacts link"
      contains: "contacts"
  key_links:
    - from: "src/components/chat/ContactForm.tsx"
      to: "/api/contact"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/contact"
    - from: "src/components/chat/MessageBubble.tsx"
      to: "src/components/chat/ContactForm.tsx"
      via: "conditional render on data part type"
      pattern: "contact-form"
    - from: "src/components/chat/ChatWindow.tsx"
      to: "src/components/chat/MessageBubble.tsx"
      via: "parts prop passed from UIMessage"
      pattern: "parts.*msg"
    - from: "src/app/admin/contacts/page.tsx"
      to: "contact_submissions"
      via: "supabase query"
      pattern: "from.*contact_submissions"
---

<objective>
Build the frontend ContactForm component, integrate it into the chat message flow, and add an admin contacts dashboard page.

Purpose: Complete the user-facing out-of-KB flow (user sees form, submits it) and the admin-facing view (admin sees all submissions). This makes the feature end-to-end functional.

Output: ContactForm component, updated MessageBubble/ChatWindow, admin contacts page with table, updated admin nav.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-out-of-kb-handling/09-RESEARCH.md
@.planning/phases/09-out-of-kb-handling/09-01-SUMMARY.md
@src/components/chat/MessageBubble.tsx
@src/components/chat/ChatWindow.tsx
@src/app/admin/layout.tsx
@src/app/admin/analytics/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactForm component + MessageBubble/ChatWindow integration</name>
  <files>src/components/chat/ContactForm.tsx, src/components/chat/MessageBubble.tsx, src/components/chat/ChatWindow.tsx</files>
  <action>
1. Create `src/components/chat/ContactForm.tsx`:
   - 'use client' component
   - Props: `{ conversationId: string; originalQuestion: string }`
   - State: `name`, `email`, `submitting`, `submitted`, `error`
   - Form with name (text, required) and email (email, required) inputs
   - Display the original question as read-only context above the form fields ("Your question: ...")
   - On submit: POST to `/api/contact` with `{ conversationId, name, email, question: originalQuestion }`
   - Success state: green confirmation box "Thanks! Our team will get back to you soon."
   - Error state: red text below form with retry option
   - Submitting state: button shows "Sending..." and is disabled
   - Styling: amber/warm tones (bg-amber-50 dark:bg-amber-900/20, border-amber-200 dark:border-amber-700)
   - Use `AlertCircle` icon from lucide-react for the header
   - Use `Send` icon from lucide-react for the submit button
   - Support dark mode with proper dark: classes matching the project's existing patterns
   - Inputs should use the same subtle styling as the rest of the chat (rounded, border-gray-300, etc.)

2. Update `src/components/chat/MessageBubble.tsx`:
   - Add `parts` prop: `parts?: Array<{ type: string; [key: string]: unknown }>`
   - Import `ContactForm` component
   - In the assistant message branch, after the text content div and before the sources section:
     - Check if any part has `type === 'data'` and the part's value array contains an item with `type === 'contact-form'`
     - AI SDK v6 data parts arrive as `{ type: 'data', data: [...] }` on the message parts array
     - Extract `conversationId` and `originalQuestion` from the contact-form data item
     - Render `<ContactForm conversationId={...} originalQuestion={...} />` in a `mt-4` wrapper
   - Keep existing Source interface and sources rendering unchanged

3. Update `src/components/chat/ChatWindow.tsx`:
   - In the messages map where `<MessageBubble>` is rendered, pass `parts={msg.parts}` as a new prop
   - `msg.parts` is already available from the UIMessage type returned by useChat
   - No other changes needed — the data part flows through automatically from AI SDK
  </action>
  <verify>
Run `npm run check` to lint all three files. Run `npm run build` to verify compilation. Verify ContactForm imports are correct.
  </verify>
  <done>
ContactForm renders inline within assistant messages when contact-form data part is present. Form submits to /api/contact and shows success/error states. ChatWindow passes parts through to MessageBubble.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin contacts dashboard page + nav link</name>
  <files>src/app/admin/contacts/page.tsx, src/app/admin/contacts/ContactsTable.tsx, src/app/admin/layout.tsx</files>
  <action>
1. Create `src/app/admin/contacts/page.tsx`:
   - Server Component with auth check (same pattern as /admin/page.tsx and /admin/analytics/page.tsx)
   - Use `getSession()` + redirect if not authenticated
   - Query `contact_submissions` from Supabase using `createServiceRoleClient()`
   - Select: `id, name, email, original_question, status, created_at, conversation_id`
   - Order by `created_at DESC`
   - Pass data to `<ContactsTable>` client component
   - Include page header: "Contact Submissions" with subtitle "Questions that couldn't be answered from the knowledge base"

2. Create `src/app/admin/contacts/ContactsTable.tsx`:
   - 'use client' component
   - Props: array of contact submission objects
   - Render a table with columns: Date, Name, Email, Question, Status
   - Status badges with color coding:
     - `pending`: amber badge (bg-amber-100 text-amber-800 / dark variants)
     - `contacted`: blue badge (bg-blue-100 text-blue-800 / dark variants)
     - `resolved`: green badge (bg-green-100 text-green-800 / dark variants)
   - Format dates using `new Date(created_at).toLocaleDateString()` with time
   - Truncate long questions with ellipsis (max ~80 chars) with title tooltip for full text
   - If no submissions: empty state "No contact submissions yet"
   - Table styling consistent with DocumentTable patterns (dark mode, responsive)
   - On mobile, switch to a card layout instead of table (responsive pattern)

3. Update `src/app/admin/layout.tsx`:
   - Add a "Contacts" navigation link between "Analytics" and the end of nav links
   - Link href: `/admin/contacts`
   - Same styling as existing nav links (text-sm, gray colors, hover states, transition-colors)
  </action>
  <verify>
Run `npm run check` to lint all files. Run `npm run build` to verify compilation. Check that admin layout contains 4 nav links (Knowledge Base, Embed Widget, Analytics, Contacts).
  </verify>
  <done>
Admin contacts page lists all contact submissions with status badges and timestamps. Admin nav includes Contacts link. Table is responsive with card layout on mobile.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end human verification of out-of-KB flow</name>
  <files>N/A</files>
  <action>
Verify complete out-of-KB handling flow: low-confidence detection in chat route streams contact-form data part, ContactForm renders inline in chat, form submits to /api/contact and saves to database, admin contacts page shows all submissions.

Steps to verify:
1. Open [http://localhost:3000/chat](http://localhost:3000/chat)
2. Ask a question clearly outside the FlowBoard knowledge base, e.g. "What is the weather in Tokyo?" or "How do I cook pasta?"
3. Verify the bot responds with "I don't have information about that in my knowledge base" text
4. Verify an inline contact form appears below the message with name and email fields
5. Fill in the form with a test name and email, then submit
6. Verify the form shows a green success confirmation
7. Open [http://localhost:3000/admin/contacts](http://localhost:3000/admin/contacts) (login if needed)
8. Verify the submission appears in the table with status "pending", your name, email, and the original question
9. Go back to [http://localhost:3000/chat](http://localhost:3000/chat)
10. Ask a question that IS in the knowledge base, e.g. "What are FlowBoard's pricing plans?"
11. Verify the bot answers normally from KB with no contact form shown
12. Verify the admin nav bar has 4 links: Knowledge Base, Embed Widget, Analytics, Contacts
  </action>
  <verify>All 12 verification steps pass</verify>
  <done>Out-of-KB handling flow works end-to-end: low-confidence queries show contact form, submissions persist, admin can view them, and KB queries still work normally</done>
</task>

</tasks>

<verification>
1. `npm run check` passes with no lint errors
2. `npm run build` succeeds
3. Out-of-KB query triggers contact form inline in chat
4. In-KB query returns normal answer without contact form
5. Contact form submission saves to database
6. Admin contacts page lists submissions with status badges
7. Admin nav includes Contacts link
</verification>

<success_criteria>
- Bot shows "I don't have information" message + contact form for out-of-KB queries
- Contact form collects name/email and submits successfully
- Admin can view all submissions at /admin/contacts
- Normal KB queries continue to work as before (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/09-out-of-kb-handling/09-02-SUMMARY.md`
</output>
