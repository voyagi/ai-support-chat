---
phase: 09-out-of-kb-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/009_contact_submissions.sql
  - src/app/api/contact/route.ts
  - src/app/api/chat/route.ts
autonomous: true

must_haves:
  truths:
    - "Low-confidence queries (similarity < 0.7) return an explicit 'I don't have information' message plus a contact-form data part instead of an LLM-generated answer"
    - "Contact form submissions are persisted to the contact_submissions table with conversation_id, name, email, original_question, and status"
    - "Invalid or incomplete contact form submissions return 400 with descriptive error"
  artifacts:
    - path: "supabase/migrations/009_contact_submissions.sql"
      provides: "contact_submissions table with FK to conversations, status check constraint, indexes"
      contains: "contact_submissions"
    - path: "src/app/api/contact/route.ts"
      provides: "POST endpoint for contact form submission"
      exports: ["POST"]
    - path: "src/app/api/chat/route.ts"
      provides: "Low-confidence branch that streams contact-form data part instead of LLM text"
      contains: "createUIMessageStream"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "searchSimilarChunks"
      via: "chunks.length === 0 check after RAG retrieval"
      pattern: "hasConfidentAnswer.*chunks"
    - from: "src/app/api/contact/route.ts"
      to: "contact_submissions"
      via: "supabase insert"
      pattern: "from.*contact_submissions.*insert"
---

<objective>
Add backend infrastructure for out-of-KB handling: database table for contact submissions, API endpoint for form submissions, and low-confidence detection in the chat route that streams a contact-form data part instead of calling the LLM.

Purpose: When RAG retrieval returns no confident matches, the bot must NOT hallucinate. Instead it returns a structured message directing users to a contact form. This is the backend half of that flow.

Output: SQL migration, contact submission API route, modified chat route with confidence branching.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-out-of-kb-handling/09-RESEARCH.md
@src/app/api/chat/route.ts
@src/lib/chat/conversation.ts
@src/lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + contact submission API endpoint</name>
  <files>supabase/migrations/009_contact_submissions.sql, src/app/api/contact/route.ts</files>
  <action>
1. Create `supabase/migrations/009_contact_submissions.sql`:
   - `contact_submissions` table with columns:
     - `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
     - `conversation_id UUID REFERENCES conversations(id) ON DELETE SET NULL`
     - `name TEXT NOT NULL`
     - `email TEXT NOT NULL`
     - `original_question TEXT NOT NULL`
     - `status TEXT NOT NULL CHECK (status IN ('pending', 'contacted', 'resolved'))` — default 'pending'
     - `admin_notes TEXT`
     - `created_at TIMESTAMPTZ DEFAULT NOW()`
   - Index on `(status, created_at DESC)` for admin dashboard queries
   - Index on `conversation_id` for conversation lookups

2. Create `src/app/api/contact/route.ts`:
   - POST handler accepting JSON body: `{ conversationId, name, email, question }`
   - Validate all required fields present and non-empty
   - Basic email validation with regex `^[^\s@]+@[^\s@]+\.[^\s@]+$`
   - Use `createServiceRoleClient()` from `@/lib/supabase/server`
   - Insert into `contact_submissions` with `status: 'pending'`
   - Return `{ success: true, id: data.id }` on success
   - Return `{ error: 'descriptive message' }` with 400 or 500 status on failure
   - DO NOT add authentication — this endpoint is called from the public chat UI
  </action>
  <verify>
Run `npm run check` to lint the new files. Verify migration SQL is syntactically valid by reading it back.
  </verify>
  <done>
contact_submissions table migration exists with all columns, constraints, and indexes. POST /api/contact validates input and inserts into database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Low-confidence detection in chat route with contact-form data part</name>
  <files>src/app/api/chat/route.ts</files>
  <action>
Modify the existing chat route to detect low-confidence RAG results and return a structured contact-form response instead of calling the LLM.

After step 5 (RAG retrieval, line ~97-100) and before step 6 (context building), add a confidence check:

```typescript
const hasConfidentAnswer = chunks.length > 0 && chunks[0].similarity > 0.7;
```

If `hasConfidentAnswer` is false, instead of proceeding to LLM streaming:

1. Import `createUIMessageStream` from `'ai'`
2. Create conversation if needed (already done at line 93-94, reuse `conversationId`)
3. Build a `createUIMessageStream` response:
   - Use `execute: ({ writer })` callback
   - Write a text part: `"I don't have information about that in my knowledge base. Let me connect you with our team who can help!"`
   - Write a custom data part with type `'data'` and value `{ type: 'contact-form', conversationId, originalQuestion: userMessage }`
   - Use `writer.write({ type: 'data', value: [{ type: 'contact-form', conversationId, originalQuestion: userMessage }] })`
4. Save messages with `answeredFromKb: false`:
   ```typescript
   saveMessages(conversationId, userMessage, "I don't have information about that in my knowledge base. Let me connect you with our team who can help!", false).catch(err => console.error("Failed to persist conversation:", err));
   ```
5. Return `stream.toUIMessageStreamResponse({ headers })` with the same headers pattern (X-Conversation-Id)
6. Do NOT include X-Sources header for low-confidence responses (no sources to cite)

The existing LLM streaming path (steps 6-9) remains unchanged — it runs when `hasConfidentAnswer` is true.

IMPORTANT: The `answeredFromKb` check on line 148 already does `chunks.length > 0 && chunks[0].similarity > 0.7`. The low-confidence branch bypasses the LLM entirely, so the `onFinish` callback won't fire for low-confidence answers. That's correct — we persist the message in the low-confidence branch separately.
  </action>
  <verify>
Run `npm run check` to lint the modified file. Run `npm run build` to verify compilation. Verify the low-confidence branch and normal branch are mutually exclusive.
  </verify>
  <done>
Chat route has two branches: (1) low-confidence (no chunks above 0.7) streams text + contact-form data part without calling LLM, (2) high-confidence proceeds with existing LLM flow. Messages are persisted in both branches.
  </done>
</task>

</tasks>

<verification>
1. `npm run check` passes with no lint errors
2. `npm run build` succeeds
3. Chat route contains `hasConfidentAnswer` check before LLM streaming
4. Low-confidence path uses `createUIMessageStream` with text + data part
5. Contact API validates input and returns proper error codes
6. SQL migration has all required columns, constraints, and indexes
</verification>

<success_criteria>
- Low-confidence RAG queries (0 chunks above 0.7) trigger the contact-form data part branch
- Contact submission POST endpoint validates and persists to database
- Both branches persist messages with correct `answered_from_kb` value
- All files lint and build cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-out-of-kb-handling/09-01-SUMMARY.md`
</output>
