---
phase: 07-embed-code-generator
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/widget/page.tsx
autonomous: false

must_haves:
  truths:
    - "Changing position in config panel updates widget position in preview iframe"
    - "Changing theme in config panel updates widget theme in preview iframe"
    - "Widget preview is fully interactive (click bubble, open chat, type messages)"
    - "Admin can navigate to /admin/embed from nav bar and dashboard CTA"
    - "Copy button copies correct embed code to clipboard"
  artifacts:
    - path: "src/app/widget/page.tsx"
      provides: "CONFIG_UPDATE postMessage listener for preview config sync"
      contains: "CONFIG_UPDATE"
  key_links:
    - from: "src/components/admin/BrowserPreview.tsx"
      to: "src/app/widget/page.tsx"
      via: "postMessage CONFIG_UPDATE"
      pattern: "CONFIG_UPDATE"
---

<objective>
Wire the widget page to receive CONFIG_UPDATE postMessage from the embed page preview, then verify the complete embed code generator flow end-to-end.

Purpose: Make the live preview responsive to config changes and verify the full user journey (navigate, configure, preview, copy).
Output: Widget page updated with CONFIG_UPDATE listener. Full feature verified by human.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-embed-code-generator/07-01-PLAN.md

Key context:
- Widget page at `src/app/widget/page.tsx` already handles THEME_UPDATE postMessage from parent
- BrowserPreview component (from 07-01) sends CONFIG_UPDATE via postMessage with { position, theme, greeting }
- Widget loader reads data-position and data-theme from script tag, but the iframe widget page doesn't currently respond to position/greeting changes at runtime
- For the preview, theme changes are the main visual update (position is handled by the loader, not the iframe page)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CONFIG_UPDATE postMessage handler to widget page</name>
  <files>
    src/app/widget/page.tsx
  </files>
  <action>
Modify the existing `handleMessage` function in `src/app/widget/page.tsx` to also handle `CONFIG_UPDATE` messages from the embed page preview:

In the existing `handleMessage` callback:
- Add a case for `type === "CONFIG_UPDATE"`:
  - Extract `config` from `event.data` (contains `{ theme, position, greeting }`)
  - If `config.theme` is "light" or "dark", call `setTheme(config.theme)`
  - Position and greeting changes don't need iframe-side handling (position is a loader concern, greeting is only for the initial prompt)

This is a minimal change — add ~5 lines to the existing message handler. The THEME_UPDATE case already exists, CONFIG_UPDATE extends it.

Example addition inside `handleMessage`:
```typescript
if (type === "CONFIG_UPDATE") {
  const { config } = event.data ?? {};
  if (config?.theme === "light" || config?.theme === "dark") {
    setTheme(config.theme);
  }
}
```

Keep the existing THEME_UPDATE handler as-is (it's used by the widget loader's postMessage bridge).
  </action>
  <verify>
Run `npm run check` — Biome passes.
Run `npm run build` — No TypeScript errors.
  </verify>
  <done>
Widget page handles CONFIG_UPDATE postMessage, updating theme when admin changes config in the embed page preview. Existing THEME_UPDATE handler preserved for widget loader compatibility.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete embed code generator flow</name>
  <files>none</files>
  <action>
Human verification of the complete embed code generator page: configuration controls,
syntax-highlighted code block, copy button, browser mockup preview, admin navigation,
and live config-to-preview sync.

Ensure dev server is running (`npm run dev`) and widget is built (`npm run build:widget`).
Then verify all behaviors listed in the done criteria.
  </action>
  <verify>
1. Start dev server: `npm run dev`
2. Navigate to `/admin/login`, log in with admin credentials
3. On the admin dashboard, verify:
   a. Nav bar shows "Knowledge Base" and "Embed Widget" links
   b. Blue "Get Embed Code" button visible in header area
4. Click "Get Embed Code" (or "Embed Widget" in nav) — should land on `/admin/embed`
5. On the embed page, verify:
   a. Two-column layout: config + code on left, browser preview on right
   b. Instruction text: "Paste this before the closing body tag on your website"
   c. Config panel has Position dropdown, Theme dropdown, Greeting textarea
6. Test code snippet reactivity:
   a. Change Position to "Bottom Left" — code snippet should add `data-position="bottom-left"`
   b. Change Theme to "Dark" — code snippet should add `data-theme="dark"`
   c. Type a greeting — code snippet should add `data-greeting="..."`
   d. Reset to defaults — extra data attributes should disappear
7. Test syntax highlighting:
   a. Tags should be pink, attributes cyan, string values green
   b. Code block background should be dark (gray-900) regardless of admin theme
8. Test copy button:
   a. Click "Copy" — icon should switch to checkmark with "Copied!" text
   b. After ~2 seconds, should revert to clipboard icon with "Copy" text
   c. Paste somewhere to verify correct code was copied
9. Test browser preview:
   a. Browser mockup should show macOS-style window controls (red/yellow/green dots)
   b. Widget should be interactive — click bubble, open chat, type message
   c. Change theme to "Dark" in config — preview widget should switch to dark mode
10. Test navigation: click "Knowledge Base" in nav — should return to admin dashboard

Type "approved" if all checks pass, or describe any issues found.
  </verify>
  <done>
All 10 verification checks pass. Embed code generator page is fully functional:
navigation works, config updates snippet reactively, syntax highlighting renders
correctly, copy button provides feedback, browser preview is interactive, and
theme changes sync to preview widget.
  </done>
</task>

</tasks>

<verification>
1. Widget page handles CONFIG_UPDATE postMessage for preview theme sync
2. Full embed page flow works end-to-end (navigate, configure, preview, copy)
3. Code snippet updates live with config changes
4. Syntax highlighting renders correctly with 4 distinct token colors
5. Copy button provides visual feedback
6. Browser preview is interactive and responds to theme changes
7. Navigation links work in both admin layout and dashboard
</verification>

<success_criteria>
- Admin can navigate to /admin/embed from nav bar or dashboard CTA
- Config changes instantly update the code snippet (no Generate button)
- Copy button copies correct code and shows checkmark for ~2 seconds
- Browser preview shows working widget that updates theme with config
- Syntax highlighting uses pink/cyan/green/gray on always-dark background
- Human verifier approves the complete flow
</success_criteria>

<output>
After completion, create `.planning/phases/07-embed-code-generator/07-02-SUMMARY.md`
</output>
