---
phase: 04-full-page-chat-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Landing page loads instantly with no signup or authentication required"
    - "Prominent CTA button links to /chat for zero-friction demo entry"
    - "Landing page is responsive across mobile, tablet, and desktop breakpoints"
    - "Feature highlights explain what the chatbot does (streaming, RAG, history)"
    - "Full flow works: landing page -> click CTA -> chat page -> send message -> streaming response appears"
    - "Chat UI is responsive on mobile (320px+), tablet (768px+), and desktop (1024px+)"
    - "Bot identity visible in chat header (Flo name, AI assistant disclaimer)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page with hero section, demo CTA, and feature highlights"
      min_lines: 40
  key_links:
    - from: "src/app/page.tsx"
      to: "/chat"
      via: "CTA link href"
      pattern: "href.*chat"
    - from: "src/app/page.tsx"
      to: "src/app/layout.tsx"
      via: "rendered inside RootLayout"
      pattern: "RootLayout"
---

<objective>
Build the landing page with zero-friction demo entry and verify the complete Phase 4 user flow end-to-end.

Purpose: The landing page is the first thing Upwork prospects see. It must communicate value instantly and provide a single-click path to the chat demo. The verification checkpoint ensures everything works together — streaming, responsiveness, bot identity.

Output: 1 updated file (landing page), plus human verification of the full flow.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-page-chat-ui/04-01-SUMMARY.md
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page with zero-friction demo CTA</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Replace the placeholder landing page (`src/app/page.tsx`) with a polished, responsive landing page. This is a Server Component (no `'use client'` needed).

**Hero Section:**
- Full viewport height hero: `min-h-screen` with subtle gradient background (`bg-gradient-to-b from-blue-50 to-white`)
- Centered content: `max-w-4xl mx-auto px-4 py-16 sm:py-24`
- Heading: "AI Support Chat" — `text-4xl sm:text-5xl lg:text-6xl font-bold text-gray-900 tracking-tight`
- Subheading: "See how AI-powered customer support works. Ask questions, get instant answers from a knowledge base — powered by RAG and streaming AI." — `text-lg sm:text-xl text-gray-600 max-w-2xl mx-auto mt-6`
- Primary CTA button: `<a href="/chat">` styled as large button — `inline-flex items-center gap-2 px-8 py-4 bg-blue-600 text-white text-lg font-semibold rounded-xl hover:bg-blue-700 transition-colors shadow-lg hover:shadow-xl`. Include lucide `MessageCircle` icon next to "Try the Demo" text.
- Below CTA: "No signup required — instant access" in small muted text (`text-sm text-gray-500 mt-4`)

**Feature Highlights Grid:**
- Section below hero: `mt-16 sm:mt-24`
- 3-column grid on desktop, stacked on mobile: `grid grid-cols-1 sm:grid-cols-3 gap-8 sm:gap-12`
- Each feature card: icon (lucide), title, description. No borders or shadows — clean, minimal style.
  - Feature 1: lucide `Zap` icon — "Streaming Responses" — "Watch answers appear in real-time, token by token. No waiting for the full response."
  - Feature 2: lucide `BookOpen` icon — "Knowledge Base RAG" — "Every answer is grounded in documentation. No hallucination, just facts from the knowledge base."
  - Feature 3: lucide `History` icon — "Conversation Context" — "Follow-up questions work naturally. The AI remembers what you discussed earlier."
- Icon styling: `w-10 h-10 text-blue-600 mb-3`
- Title: `text-lg font-semibold text-gray-900 mb-2`
- Description: `text-gray-600 leading-relaxed`

**Footer/Attribution:**
- Small footer below features: "Built as a portfolio demo — " followed by a link to the admin panel: `<a href="/admin">Admin Panel</a>` styled as subtle underlined link
- "Powered by Next.js, OpenAI, and Supabase" — `text-xs text-gray-400 mt-2`

**Responsive Design:**
- Mobile (< 640px): Single column, smaller text sizes, tighter padding
- Tablet (640px+): Grid starts appearing, text scales up
- Desktop (1024px+): Full 3-column grid, largest text sizes
- All breakpoints use Tailwind responsive prefixes (`sm:`, `md:`, `lg:`)

**Metadata:**
- Export `metadata` object: `title: "AI Support Chat — Live Demo"`, `description: "Interactive AI customer support chatbot demo. Powered by RAG, streaming AI, and a knowledge base. Try it instantly — no signup required."`
  </action>
  <verify>
Run `npm run build` — TypeScript compiles with no errors. Run `npm run check` — Biome lint and format pass. Verify `src/app/page.tsx` contains `href="/chat"` link. Verify the file exports metadata and a default component.
  </verify>
  <done>
Landing page renders with hero section, prominent CTA linking to /chat, 3 feature cards, responsive layout, and proper metadata. File passes TypeScript compilation and Biome checks.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 4 flow</name>
  <what-built>
Full-page chat UI with streaming responses, bot identity, responsive design, and landing page with zero-friction demo entry. All components from Plans 04-01 and 04-02.
  </what-built>
  <how-to-verify>
Start the dev server (`npm run dev`) and verify the following:

**1. Landing Page (http://localhost:3000)**
- [ ] Hero section with "AI Support Chat" heading loads
- [ ] "Try the Demo" CTA button is visible and prominent
- [ ] "No signup required" text visible below CTA
- [ ] 3 feature cards display (Streaming, RAG, Context)
- [ ] Page is responsive: resize browser from desktop to mobile width

**2. Click CTA → Chat Page (http://localhost:3000/chat)**
- [ ] Chat page loads with header showing "Flo" name and "AI assistant" disclaimer
- [ ] Welcome message or empty state is visible
- [ ] Input area is at the bottom with a text field and send button

**3. Send a Message**
- [ ] Type "What is FlowBoard?" and press Enter
- [ ] Typing indicator (animated dots) appears while waiting
- [ ] Response streams in token-by-token (not all at once)
- [ ] Response is grounded in FlowBoard knowledge base (mentions PM tool, pricing, features)
- [ ] User message appears right-aligned in blue
- [ ] Assistant message appears left-aligned with bot avatar

**4. Follow-up Question**
- [ ] Type "How much does the Pro plan cost?" and send
- [ ] Response references FlowBoard Pro pricing ($12/user/month)
- [ ] Previous messages remain visible above

**5. Responsive Design**
- [ ] Open browser DevTools → toggle device toolbar
- [ ] Check at 375px width (iPhone SE): messages readable, input usable, no horizontal scroll
- [ ] Check at 768px width (tablet): layout adjusts properly
- [ ] Check at 1440px width (desktop): messages centered, not stretched too wide

**6. Error Recovery (optional — if API key not set or service down)**
- [ ] If error occurs, error banner with "Retry" button appears
- [ ] Clicking retry re-sends the message
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `npm run check` exits 0
3. Landing page (/) has prominent CTA linking to /chat
4. Chat page (/chat) shows bot identity header
5. Messages stream token-by-token
6. UI is responsive across mobile/tablet/desktop
7. Human verified the complete flow
</verification>

<success_criteria>
- Landing page provides instant, zero-friction access to chat demo via prominent CTA
- Complete user flow works: landing -> chat -> send message -> streaming response
- Bot identity (Flo, AI assistant disclaimer) is visible in chat header
- UI is responsive across all breakpoints (mobile 375px through desktop 1440px)
- Human has verified and approved the full Phase 4 experience
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-page-chat-ui/04-02-SUMMARY.md`
</output>
